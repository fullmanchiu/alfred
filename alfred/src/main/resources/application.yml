spring:
  application:
    name: alfred

  # Jackson 配置
  jackson:
    property-naming-strategy: lower_camel_case
    parameter-names-used: true

  # 数据库配置
  datasource:
    # PostgreSQL 数据库
    url: jdbc:postgresql://110.42.222.64:35432/alfred
    driver-class-name: org.postgresql.Driver
    username: alfred
    password: 7j5xS8ENKZe74Hde
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-test-query: SELECT 1

  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update  # 开发模式：自动创建表
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect  # PostgreSQL 方言
    show-sql: true
    format-sql: true

  # Flyway 配置
  flyway:
    enabled: true  # 启用数据库迁移
    baseline-on-migrate: true
    locations: classpath:db/migration

  # Redis 配置（可选）
  redis:
    host: localhost
    port: 6379

# 服务器配置
server:
  port: 8080
  servlet:
    context-path: /

# JWT 配置
jwt:
  secret: alfred-secret-key-change-this-in-production
  expiration: 1800000  # 30分钟（毫秒）

# 日志配置
logging:
  level:
    com.colafan: DEBUG
    org.springframework.security: DEBUG
